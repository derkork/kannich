# Maven Resolver TransporterFactory implementations for artifact downloads.
# These are discovered via Java SPI and used by MavenDependenciesResolver.
# maven-resolver-transport-* JARs use Sisu DI (not SPI) internally, so we
# register them explicitly here for ServiceLoader-based discovery.
org.eclipse.aether.transport.http.HttpTransporterFactory
org.eclipse.aether.transport.file.FileTransporterFactory
org.eclipse.aether.transport.wagon.WagonTransporterFactory
